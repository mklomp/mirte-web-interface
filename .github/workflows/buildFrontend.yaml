name: Build site

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt install -y python3-pip python3-setuptools python3-wheel
          sudo -H pip install nodeenv
          nodeenv --node=22.12.0 node_env
          source node_env/bin/activate
          cd vue-frontend
          npm install
      - name: Check licence compatability
        run: |
          curl -sSfL https://get.anchore.io/syft | sudo sh -s -- -b /usr/local/bin
          syft . -o spdx-json > sbom.spdx.json
          cat sbom.spdx.json
          npm ls --omit=dev --depth=0 --json > test.json
          cat test.json
          npm ls --omit=dev --depth=0 --json | jq '.dependencies | keys' > deps.json
          cat deps.json
          jq --slurpfile deps deps.json '
            def clean_license:
              (
                .licenseDeclared,
                .licenseConcluded
              )
              | select(. != null)
              | select(. != "NOASSERTION");

            .packages
            | map(
                select(.name as $n | $deps[0] | index($n))
                | {
                    name,
                    version: .versionInfo,
                    license: (
                      clean_license
                      // (.licenses[]?.license?.id?)
                      // (.licenses[]?.license?)
                      // "UNKNOWN"
                    ),
                    website: (.homepage // .downloadLocation)
                  }
              )
          ' sbom.spdx.json > licenses.json
          cat licenses.json
      - name: Build
        run: |          
          npm run generate
          rm .output/nitro.json
          cp sbom.spdx.json .output
          cp licenses.json .ouput
      - name: Archive production artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: |
            vue-frontend/.output
      - name: Upload as a github-pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: vue-frontend/.output/public

  deploy:
    needs: build
    if: github.ref == 'refs/heads/develop'
    permissions:
      pages: write      # to deploy to Pages
      id-token: write   # to verify the deployment originates from an appropriate source
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        # with:
        #   artifact_name: dist
